{
  "name": "copy-and-replace",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build": "esbuild --bundle --platform=node --outdir=lib src/index.js",
    "test": "TEST=Walshy node src/index.js"
  },
  "author": "BlobDevelopment",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.9.1"
  },
  "devDependencies": {
    "esbuild": "^0.14.8"
  }
}
